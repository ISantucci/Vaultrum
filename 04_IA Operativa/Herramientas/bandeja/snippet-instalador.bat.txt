:: ============================================================
::  Bandeja / Observer - capa IA Operativa
::  Canal entre el productor (Cowork) y los ejecutores (Claude Code)
:: ============================================================
set "VAULTRUM=D:\git\Vaultrum"
set "BANDEJA=%VAULTRUM%\04_IA Operativa\Herramientas\bandeja"

echo.
echo [Bandeja] preparando canal de ordenes...

if not exist "%BANDEJA%"             mkdir "%BANDEJA%"
if not exist "%BANDEJA%\ordenes"     mkdir "%BANDEJA%\ordenes"
if not exist "%BANDEJA%\resultados"  mkdir "%BANDEJA%\resultados"
if not exist "%BANDEJA%\procesadas"  mkdir "%BANDEJA%\procesadas"

:: Si tu instalador copia archivos desde un origen, descomenta y ajusta:
:: copy /Y "%ORIGEN%\observer.ps1" "%BANDEJA%\observer.ps1" >nul
:: copy /Y "%ORIGEN%\observer.bat" "%BANDEJA%\observer.bat" >nul
:: copy /Y "%ORIGEN%\README.md"    "%BANDEJA%\README.md"    >nul

where claude >nul 2>&1
if errorlevel 1 (
    echo   [!] 'claude' no esta en el PATH: el observer va a arrancar pero no va a poder ejecutar ordenes.
) else (
    echo   [ok] claude encontrado en el PATH
)

if exist "%BANDEJA%\observer.ps1" (
    echo   [ok] observer instalado en: %BANDEJA%
    echo       para arrancarlo: "%BANDEJA%\observer.bat"
) else (
    echo   [!] falta observer.ps1 en la bandeja
)
